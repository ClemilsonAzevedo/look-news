import { Tray, Menu, nativeImage, app, type BrowserWindow } from 'electron'import path from 'node:path'import { positionWindowNearTray } from './windows'export function createTray(mainWindow: BrowserWindow): Tray {    const iconName = process.platform === 'darwin' ? 'trayIconTemplate.png' : 'trayIcon.png'    const iconPath = path.join(app.getAppPath(), 'assets', iconName)    const tray = new Tray(nativeImage.createFromPath(iconPath))    tray.setToolTip('look news')    tray.on('click', () => {        if (mainWindow.isVisible()) {            mainWindow.hide()            return        }        positionWindowNearTray(mainWindow, tray.getBounds())        mainWindow.show()        mainWindow.focus()    })    const contextMenu = Menu.buildFromTemplate([{ label: 'Sair do look news', click: () => app.quit() }])    tray.on('right-click', () => tray.popUpContextMenu(contextMenu))    return tray}