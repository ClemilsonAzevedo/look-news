import './screens/theme.css'import { mountArticlesScreen } from './screens/articles/view'import { mountSourcesScreen } from './screens/sources/view'import { mountAboutScreen } from './screens/about/view'const root = document.getElementById('app')function render(): void {    if (!root) return    const route = location.hash.replace(/^#\/?/, '') || 'articles'    root.innerHTML = ''    if (route === 'sources') mountSourcesScreen(root)    else if (route === 'about') mountAboutScreen(root)    else mountArticlesScreen(root)}window.addEventListener('hashchange', render)render()